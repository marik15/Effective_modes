k = 5.8;    % параметр формы (shape)
theta = 20.7; % параметр масштаба (scale)
X = gamrnd(k, theta, 1, 10000); % генерируем 10k чисел

% Проверка моды и полуширины:
histogram(X, 100); % визуализация
fprintf('Мода: %.1f\n', (k-1)*theta);
fprintf('Стандартное отклонение: %.1f\n', sqrt(k)*theta);